You are running one bounded autonomous implementation tick inside C:\00_Codes\illusion-3d.

User goal: implement `project_proposal.md` for the KAIST 3D Rendering Contest and keep iterating for 3 hours until the goal is as complete as possible.

Hard rules:
- Do NOT ask the user questions. Choose the highest-value next slice that fits one tick.
- Do NOT create another cron/background runner. This tick is already inside the runner.
- Keep KAIST rules first: no external 3D assets, no Blender rendering, no commercial/closed 3D tools, procedural/browser WebGL output.
- Preserve evidence artifacts; do not destructively clean screenshots/logs.
- Append a concise tick entry to `.hermes/plans/perceptual-twin-room-3h-log-2026-05-17.md`.

Read first:
- `project_proposal.md`
- `.hermes/plans/perceptual-twin-room-3h-2026-05-17.md`
- `.hermes/plans/perceptual-twin-room-3h-log-2026-05-17.md`

Current baseline already has a working MVP. Pick the next unfinished slice in this priority order:
1. UI state and overlay polish: active/toggled button states, separate or reduce ray/frustum/ghost/wire clutter, labels.
2. Visual quality pass: stronger composition, labels, text readability, room silhouette, silent-view understanding.
3. Capture/export polish: robust 10s WebM behavior and ffmpeg MP4 instructions.
4. README and write-up draft with formulas, rules, reproduction, references.
5. Final QA and artifact size checks.

Per tick workflow:
1. Inspect current files and latest log.
2. Implement exactly one coherent slice.
3. Run `npm run harness`.
4. If visual behavior changed, run/inspect the browser at `http://127.0.0.1:5173`; save a uniquely named screenshot under `artifacts/` when possible.
5. Append log entry: slice, changed files, verification result, known issue, next slice.
6. End with a short Korean summary.

If the harness fails, fix the root cause if bounded. If not bounded, log the failure and leave exact recovery steps. Do not claim completion unless verified.
