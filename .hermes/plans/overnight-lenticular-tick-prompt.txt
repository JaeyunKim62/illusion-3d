You are running an unattended bounded development tick for jaeyu's KAIST 3D Rendering Contest project.

Repo: C:/00_Codes/illusion-3d
Branch: lenticular-shared-point-cloud
Plan: .hermes/plans/overnight-lenticular-shared-cloud-2026-05-18.md
Log: .hermes/plans/overnight-lenticular-log-2026-05-18.md

Critical invariant: this project is now a 3D lenticular point cloud. There must be exactly one physical point set / one shared BufferGeometry. Do not implement separate text meshes, duplicate point clouds, per-view hidden layers, or opacity/depth-test tricks that fake different readings. Every visible reading must come from the same points' coordinates. The current proven 2-view invariant is front projection (x,y) => WHAT WE SEE and right projection (z,y, with the viewer's camera convention already handled in code) => WHAT EXISTS.

Start by reading the plan/log and checking git status. Then select exactly one coherent vertical slice from the plan backlog that can be completed and verified in this tick. Prefer slices that improve contest quality while preserving the invariant: legibility/density tuning with measurable screenshots, invariant documentation/writeup, deterministic metrics, capture/evaluation helpers, or clearly labelled experimental 4-view/color/light scaffolding.

Hard rules:
- No destructive cleanup. Do not delete artifacts; use unique output dirs.
- Do not leave broken code committed.
- Run `npm run build` before any commit.
- If browser tools are available, visually verify front and right views again. If not, state that browser QA was not available and keep the change to non-visual/documented improvements or terminal-verifiable code.
- Commit only green coherent changes. Append the log with slice, files, verification, commit hash or blocker, and next step.
- Keep the final response concise and Korean-first: why this tick ran, what changed, verification, commit/blocker, next action.
